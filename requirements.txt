# Air-Raid Alert Risk Classification — dependencies
#
# Compatible-release pins: lower bound = exact versions this project was
# tested with (so the documented results reproduce), upper bound = next major
# (so it stays installable across environments without surprise breaking changes).
#
# Tested on Python 3.13 with:
#   pandas 3.0.2 · scikit-learn 1.8.0 · numpy 2.4.4 · matplotlib 3.10.8
#
# Install:  pip install -r requirements.txt

pandas>=2.0,<4.0
scikit-learn>=1.3,<2.0
numpy>=1.24,<3.0
matplotlib>=3.7,<4.0
